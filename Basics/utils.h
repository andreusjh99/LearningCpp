#pragma once //this is to define the utils file and ensures that this file is only defined once.

//These are the prototypes
void show_menu();
int make_selection();
bool process_selection(int option);